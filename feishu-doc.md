# 飞书文档：PayContext Rules Manager 插件项目解析

**项目名称**：PayContext Rules Manager
**项目定位**：Cursor / VS Code 智能提示规则自动化管理插件

---

## 1. 主要用来做什么？

**核心目的：让 Cursor AI 自动且精准地理解复杂的业务领域知识（如支付逻辑、币种精度、合规要求），告别手动复制粘贴 Prompt。**

在日常开发中，尤其是支付、金融等对准确性和合规性要求极高的领域，AI 常常会犯一些“领域常识性”错误（例如用浮点数处理金额、在日志里明文打印卡号）。
PayContext Rules Manager 是一个 **VS Code 插件**，它允许开发者在本地维护一个“业务规则库”（Prompt 库），并提供可视化界面一键切换当前的业务场景，**插件会自动将这些规则注入到 Cursor AI 的上下文中**，让 AI 在写代码时严格遵守这些领域规范。

---

## 2. 主要体现在什么环节？

这个插件的作用贯穿了整个研发周期，主要体现在以下三个环节：

*   **开发准备环节（上下文切换）**：
    当开发者准备开发一个特定功能（例如：对接巴西 PIX 支付）时，可以通过快捷键（`Cmd+Shift+Alt+P`）或侧边栏，一键勾选“Brazil PIX 支付规则”和“币种精度规则”。
*   **AI 编码与对话环节（核心干预）**：
    在按下 `Cmd+L`（对话）或 `Cmd+K`（内联生成）与 Cursor 交互时，AI 会**自动携带**之前选中的规则。开发者只需要说“写一个支付创建接口”，AI 就会自动结合 PIX 的要求和币种规范生成合格的代码，无需在提示词中重复强调。
*   **团队协作与知识沉淀环节（资产共享）**：
    团队中的资深开发/架构师可以将踩过的坑、安全红线写成规则文件（Markdown 格式），存放在项目的 `.paycontext/rules/` 目录下。该目录可随 Git 提交，新员工 clone 代码后，直接获得了整个团队沉淀的 AI 规则资产。

---

## 3. 可以达到什么效果？

*   **“双重加持”的 AI 上下文增强**：
    *   **主动注入（强制生效）**：插件修改 `.cursorrules` 文件，保证核心规则在每次对话中必然被携带，绝不遗漏。
    *   **语义索引（智能补充）**：项目本地的 `.md` 规则文件会被 Cursor 的代码库索引（Codebase Indexing）捕捉，当对话涉及相关词汇时，AI 会主动提取更详细的规则内容。
*   **大幅降低 AI 幻觉与代码重构率**：通过硬性规则约束（如“禁止使用浮点数”、“PCI DSS 卡号脱敏规范”），确保 AI 第一次生成的代码就是符合行业标准和公司规范的。
*   **释放生产力，提升研发体验**：省去了每次对话都要写长篇大论前置条件的麻烦，“一键切换场景，即刻开始编码”。
*   **实现“提示词工程”的代码化与团队化**：Prompt 不再是个人聊天记录里的片段，而是变成像代码一样可管理、可版本控制、可视化编辑的数字资产。

---

## 4. 具体怎么实现？

虽然 Cursor 核心是闭源的，没有提供直接修改聊天框的 API，但本插件巧妙利用了其底层机制和 VS Code 插件生态来实现：

### 🔧 核心工作原理
*   **底层机制利用**：Cursor 会实时监听项目根目录下的 `.cursorrules` 文件并作为系统提示词（System Prompt）。插件的核心动作就是**动态读写该文件**。
*   **非破坏性注入**：插件在 `.cursorrules` 中划分了特定的标记块（`# ===== PayContext Rules =====`）。在注入或追加规则时，使用正则匹配仅替换插件管辖的区域，**完美保留用户自己在该文件里写的其他内容**，并提供自动备份机制（`.cursorrules.backup`）。

### 📂 存储层实现
*   **本地化 Markdown 存储**：所有规则以 `.md` 格式保存在项目根目录的 `.paycontext/rules/` 下。
*   **Frontmatter 元数据**：每个 Markdown 文件头部包含 YAML 格式的元数据（ID、名称、分类、标签等），便于插件解析和检索，正文部分则是具体的规则约束。
*   **状态记录**：通过同目录下的 `.meta.json` 记录当前被激活注入的规则 ID 列表。

### 🖥 UI 与交互层实现
*   **侧边栏树形视图 (TreeView)**：通过 VS Code API 注册左侧活动栏面板，按“分类”（支付、合规、错误处理等）可视化展示规则库及当前注入状态。
*   **Webview 规则编辑器**：内置了一个基于 HTML/CSS/JS 的 Webview 表单，开发者可以直接在编辑器内新建、修改规则，支持快捷插入约束模板和字数统计。
*   **快捷选择面板 (QuickPick)**：提供了全局快捷指令和搜索面板，支持键盘全键盘操作，实现多选、替换注入或追加注入。
*   **状态栏指示器**：在编辑器左下角实时显示当前已注入的规则数量，点击可快速切换。

### 📦 预置资产
*   插件启动时，自带一个模板初始化器（Template Initializer），内置了根据支付行业真实场景编写的 6 个标准模板（如 ISO 4217 精度、PCI-DSS 合规、日本/巴西支付特性等），帮助用户实现“开箱即用”。

---
*你可以直接将上方内容复制，粘贴至飞书文档中，飞书会自动识别 Markdown 格式并进行排版。*