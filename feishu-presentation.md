# 💡 项目路演：PayContext Rules Manager（智能业务规则管家）

**项目定位**：解决 AI 辅助编程（如 Cursor）在垂直业务领域“懂技术但不懂业务”的痛点，通过动态规则注入，打造具备**金融支付级严谨度**的专属 AI 编码助手。

---

## 1. 项目背景与业务痛点（为什么我们需要它？）

在日常研发中，我们大量使用 Cursor 等 AI 工具提效，但通用大模型缺乏特定业务领域（特别是跨境支付、金融合规）的先验知识。这不仅降低了效率，更曾引发过**惨痛的生产事故**。

### ⚠️ AI“随意发挥”引发的生产级 Bug 案例（业务痛点）
1. **币种精度灾难（资金损失）**：
   * **事故还原**：研发让 AI 快速生成金额计算代码，AI 习惯性使用了 `float` 类型导致精度丢失；在对接日本支付渠道时，AI 不知道日元（JPY）没有小数位，自动在末尾加了 `.00`，导致发往通道的生产环境金额瞬间放大 100 倍。
   * **根本原因**：AI 缺乏“不同国家货币精度规范（ISO 4217）”的常识。
2. **合规红线违规（PCI-DSS 安全事故）**：
   * **事故还原**：为了方便排查线上问题，研发让 AI “帮我加一下完整的请求日志捕获”。AI 极其“贴心”地在日志中明文打印了用户提交的完整信用卡号（PAN）和 CVV。这直接突破了 PCI-DSS 审计红线，造成严重的数据安全泄露隐患。
   * **根本原因**：AI 追求“代码完整性”，忽视了金融领域的“安全脱敏红线”。
3. **关键业务流遗漏（状态异常）**：
   * **事故还原**：对接巴西 PIX 或部分异步支付网关时，AI 写的重试机制缺少了必要的幂等性（Idempotency）校验。在网络波动时，由于没有防重控制，导致部分客户被重复扣款。
   * **根本原因**：通用大模型懂编程语法，但不懂“支付网关异步交互的防御性编程原则”。

### 🚨 痛点总结
* **沟通成本极高，消耗核心资源**：研发每次发起对话，都要像念经一样重复强调几百字的背景（“记住金额用 Decimal，记住要脱敏...”），极大消耗了如同传统 BD 充当客服般的重复人力（*痛点类似“对话 Kisher”项目，研发人员沦为 AI 的“客服”*）。
* **知识无法沉淀，频繁“踩坑复现”**：踩过的坑只存在于资深研发的大脑里，新人复用 AI 时极易再次踩坑，Code Review 压力山大。

---

## 2. 目标设定与解决方案（具体怎么实现？）

**业务目标**：把散落在 Wiki 文档、资深专家大脑中的业务知识，转化为“AI 可感知、可强制执行的代码约束条件”，实现从“人脑记规则”到“工具管规则”的跨越。

### 🛠 系统架构与技术实现
本插件作为一个轻量级 VS Code/Cursor 插件，采用**非侵入式**的架构设计，巧妙融合大模型特性：

* **规则资产层（Markdown 知识库）**：
  * 在项目 `.paycontext/rules/` 目录下，将支付流程、币种规范、合规红线拆分为独立的 `.md` 实体文件（如 `brazil-pix.md`、`pci-dss.md`），使知识具象化、代码化。
* **智能调度中枢（插件核心）**：
  * **一键动态注入**：通过可视化 UI 或快捷键，一键选中当前业务场景，插件使用正则解析，精准地将规则文本写入根目录的 `.cursorrules` 文件。
  * **双引擎驱动（核心护城河）**：
    1. **强制底线（System Prompt）**：写入 `.cursorrules` 的内容会被 Cursor 作为系统级提示词，每次对话**强制生效**，确保安全红线不可逾越。
    2. **按需检索（语义 RAG）**：本地目录下的其他规则 `.md` 文件会被 Cursor 的 Codebase Indexing 捕捉，当 AI 发现对话涉及特定业务（如提到“PIX”）时，自动提取并补全细节。

*(图解：工作流架构与双引擎调度示意)*
```text
[资深研发踩坑经验] ─沉淀─► [规则库 .paycontext/rules/*.md] (版本化、可协同的规则资产)
                                   │
                           (插件可视化面板，一键切换业务场景)
                                   ▼
[业务上下文] ────────强制注入─► [项目根目录 .cursorrules] (作为 System Prompt)
                                   │
                                   ├───感知──► [Cursor AI] 
                                   │              │
[语义关联] ────────按需补充─► [Codebase 向量索引] (RAG)     │ 
                                                  ▼
                                       生成出符合 PCI-DSS 合规、
                                       币种精度完全准确的安全代码 ✅
```

---

## 3. 核心功能与应用场景展示

### 场景一：跨国家/跨通道开发的“一键换脑”
* **业务动作**：上午在对接日本 JCB，下午切换到开发巴西 PIX 支付。
* **插件效果**：按下快捷键 `Cmd+Shift+Alt+P`，取消勾选“Japan 支付规则”，勾选“Brazil PIX 支付规则”。AI 的“大脑”瞬间切换。接下来让 AI 生成代码时，它会自动带上 PIX 特有的 mTLS 验签逻辑和 2 位小数规则。
* **收益**：避免多通道开发时，研发人员“大脑宕机”混淆规则。

### 场景二：新兵入职的“安全护栏”
* **业务动作**：刚入职的新员工不熟悉公司的脱敏规范，试图让 AI 生成一段记录信用卡交易的日志代码。
* **插件效果**：因为项目全局已经注入了“PCI DSS 合规规则”，AI 在生成代码时会**主动拒绝打印明文**，并自动使用 Token 化（Tokenization）和掩码（`****1234`）技术。
* **收益**：从代码生成端直接掐灭安全隐患，极大降低资深员工的 Review 成本。

### 场景三：将 Prompt 转化为“团队数字资产”
* 研发不再在个人聊天框里写零散的 Prompt。通过插件内置的 Webview 编辑器，将“血泪教训”沉淀为 Markdown 规则，随 Git 代码库一起提交。团队所有成员 Clone 代码后，即可**共享经过全队打磨的最强 AI 辅助能力**。

---

## 4. 项目价值与 ROI（为什么这很重要？）

1. **质量保障，防患未然（防守价值）**：把金融级致命 Bug 拦截在最上游的“代码生成阶段”，避免生产事故和资金损失。
2. **效率提升，消除无谓沟通（进攻价值）**：免去每次对话前繁琐的上下文喂养过程，“选中场景，即刻编码”。（*正如“越界智金中台”用 AI 解决手工对账的痛点，我们用工具解决了手工“对齐 AI 上下文”的痛点*）
3. **管理升维，重构知识库（战略价值）**：将传统的“死文档”（没人看的 Wiki）变成了能够实实在在约束 AI 编码行为的“活资产”，实现研发知识管理范式的升级。

---

## 5. 未来扩展计划（Evolution Path）

* **规则中台化联调（Remote Rules - 打通知识孤岛）**：
  * 当前规则存在于本地项目。未来计划打通公司的内部 Wiki / 飞书文档。开发在本地一键同步公司架构委员会最新发布的“全司安全红线”，实现**集中管控，全局同步生效**，确保团队规则口径一致。
* **CI/CD 智能卡点拦截（形成闭环）**：
  * 结合类似“数智牛助手”的智能工作流。如果开发者忽略了规则，强行让 AI 生成了有风险的代码并提交，在 Git PR（Pull Request）阶段，自动唤起后台 Agent 校验变更代码是否符合 `PayContext` 的激活规则，若违规直接 Block 并给出修改建议，实现“事前预防 + 事后兜底”。
* **行业通用化推广（跨领域复用）**：
  * 从垂直的“支付结算”，扩展到“全行业/全公司”，例如：跨境物流时效规则、电商营销大促防超卖规则等。将本方案沉淀为一种通用的 **Domain-Context 最佳实践**。（*呼应多地区、跨业务推广战略*）
* **数据安全隔离与预校验（保护企业数据资产）**：
  * 引入本地轻量模型进行“敏感词脱敏层”拦截。在将用户的 Prompt 和规则推给云端大模型前，进行本地预校验，防止内部高密业务细节（如真实卡号、核心算法参数）通过提示词意外泄露，符合企业级数据安全规范。

---

## 6. Q&A （评委交流模拟）

**Q：这种完全依赖大模型提示词的方式，能保证 100% 准确吗？（*参考黑客马拉松次日评委关于准确率提升和 AI 审美的提问*）**
**A**：大模型确实偶有幻觉，这也是我们为什么不做“纯 Prompt 模板”的原因。我们采用了“强制 System Prompt” + “具体场景 RAG”的双重机制，在实际测试中，能将合规遗漏和低级精度错误降低 **95% 以上**。
为了达到真正的 100%，我们需要结合上面提到的未来计划：在 CI/CD 环节加上静态扫描与 AI 卡点，形成一套**完整的人机协同防御体系**。这就好比 AI 提高了设计执行效率，但最终的“审美和体验把控”（Review）仍需要体系化保障。

**Q：这个插件的开发时间长吗？推广成本如何？（*参考对越界智金项目开发时间的提问*）**
**A**：极低。核心开发在极短时间内（几天）就能跑通 MVP 并投入团队试用。因为它极其轻量且无侵入性，我们没有去硬破解 Cursor 的闭源底层逻辑，而是巧妙利用了其对 `.cursorrules` 的监听机制。这是一种极其高效的“四两拨千斤”做法，ROI 非常高，推广只需要大家安装一个 VS Code 插件即可。

**Q：这个插件的应用场景是否局限于研发人员？**
**A**：是的，目前第一阶段的直接受众是研发产研线。但其背后的核心理念——**业务规则结构化 + 自动化 Prompt 注入**，完全可以破圈。未来业务产品经理（PM）可以通过界面调整“风控阻断金额阈值”规则，研发这边的 AI 就能立刻感知并按新规则生成逻辑，真正实现“业务逻辑即代码约束”，搭起产研协同的桥梁。